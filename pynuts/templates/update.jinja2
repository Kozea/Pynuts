<form method="post" enctype="multipart/form-data">
  {% for field in obj.update_form %}
    {% if not field.flags.no_label %}
      {% if field.errors %}
        {{ field.label(class="error", title=', '.join(field.errors)) }}
      {% else %}
        {{ field.label() }}
      {% endif %}
    {% endif %}
    {{ field() | safe }}
  {% endfor %}
  <input type="submit" />
</form>
